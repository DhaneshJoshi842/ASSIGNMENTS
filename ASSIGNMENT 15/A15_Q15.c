// created on 31/10/22 to print the first n prime numbers
#include <stdio.h>
int npn(int); // remember from now in the prototypes we just have to mention the type of arguments we wrinting in the bracket
int main()
{
    int a, b;
    printf("how many prime numbers you wants to print");
    scanf("%d", &a);
    printf("2 ");
    b = npn(a);
}
int npn(a)
{
    int x, y = 2;
    for (int i = x; i > 0; i--)
    {
        x = y + 1;
    }
}
